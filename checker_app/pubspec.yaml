name: checker_app
description: "MVC CSG Attendance — Checker app (offline-first scanning, roster cache, sync queue, kiosk mode)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  mobile_scanner: ^6.0.11
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sg-logo.png
