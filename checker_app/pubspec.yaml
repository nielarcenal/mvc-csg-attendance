name: checker_app
description: "MVC CSG Attendance — Checker app (offline-first scanning, roster cache, sync queue, kiosk mode)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  mobile_scanner: ^6.0.11
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.15.4

# app_links >= 6.4 needs a newer Android Gradle Plugin than Flutter 3.24.5
# projects use; 6.3.x builds fine and satisfies supabase_flutter.
dependency_overrides:
  app_links: 6.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sg-logo.png
